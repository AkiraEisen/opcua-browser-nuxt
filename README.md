```shell
node -v
# if node not exists, install
# https://nodejs.org/en

pnpm -v
# if pnpm not exists
npm install -g pnpm

# install dependencies
pnpm install

# run server locally
pnpm dev

# open chrome, http://localhost:3000/opcua
```
